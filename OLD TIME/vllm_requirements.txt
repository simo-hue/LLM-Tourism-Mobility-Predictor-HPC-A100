# VLLM Requirements for LLM-Mob Tourism Mobility Prediction
# Install with: pip install -r vllm_requirements.txt

# Core VLLM for 4x A100 tensor parallelism
vllm>=0.6.0

# VLLM dependencies that might be needed
transformers>=4.39.0
torch>=2.0.0
tokenizers>=0.15.0

# Optional performance boosters
flash-attn>=2.0.0  # Optional: Flash Attention for even better performance
ray>=2.0.0         # Optional: Ray for distributed processing (if needed)

# Make sure existing dependencies are compatible
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm>=4.65.0